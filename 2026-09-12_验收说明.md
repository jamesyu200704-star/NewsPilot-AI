# NewsPilot 本轮验收说明

## 成品定位

这是一个用于把报道线索整理成采访与核查计划的 Beta 工程，不是替记者采访、替编辑作判断或替代事实核查的工具。

## 本轮修复

- 失败、仅元数据或无摘录的抓取结果不再可以支持 `verified` 主张。
- 两条间接二手报道不再可以被当作两个独立的高质量二手来源。
- 报道提纲中的证据必须同时属于该提纲段落并显式关联当前主张。
- 提交检查重新计算关键主张的证据矩阵，不再信任可能过期的缓存。
- 页面明确显示：目前只完成开发者测试，真实用户验证为 0。

## 本轮核验

- 自动化测试：`npm test`，服务端 178 项、界面 32 项通过。
- 构建：`npm run build` 通过。
- 人工主路径：在本地界面填写“校园夜间摆渡车调整后，学生出行是否更方便”，已进入“报道策划结果”。结果将未核实内容列为未知，将判断列为工作假设，并提示采访对象不足的执行风险。

## 运行

```powershell
npm install
npm run dev
```

本轮工程使用既有依赖 Junction；如在另一台机器使用，请先正常安装依赖。默认开发页面为 `http://127.0.0.1:5173/`。

## 尚未达到

- 没有真实记者或编辑的可用性测试数据。
- 没有接入后即可直接发表的事实内容；输出仍需记者独立采访和核验。
