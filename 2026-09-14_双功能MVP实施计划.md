# NewsPilot 双功能 MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 NewsPilot 首页改为“生成采访稿”和“新闻编辑”两个基于用户已有材料的本地模型工具。

**Architecture:** 新建独立的编辑任务领域契约和 `/api/editor` 路由，不复用旧的选题策划 `GenerationResult`。前端只向新接口发送任务类型、编辑动作和用户粘贴的原文；服务端根据任务类型构造严格提示词，调用既有 Qwen/Ollama Provider，并对结果结构和“不得补造事实”边界做校验。

**Tech Stack:** React 19、TypeScript、Vite、Express 5、AJV、Vitest、Node Test、Ollama/Qwen。

---

## 文件结构

- 新建 `shared/编辑任务模型.ts`：请求、响应、任务类型和 JSON Schema。
- 新建 `prompts/编辑任务提示词.ts`：采访稿与新闻编辑的不同提示词。
- 新建 `server/services/editor.ts`：调用 Provider 并进行结果校验。
- 新建 `server/routes/editor.ts`：输入校验、限流和 HTTP 响应。
- 新建 `src/services/编辑服务.ts`：浏览器请求与本地 Mock 降级。
- 新建 `src/components/双功能MVP.tsx`：两个入口、材料输入和结果展示。
- 修改 `server/app.ts`、`src/App.tsx`、`src/styles.css`：挂载接口与替换首页。
- 新建 `src/components/双功能MVP.test.tsx`、`server/tests/editor.test.ts`、`server/tests/editorApi.test.ts`：覆盖核心边界。

### Task 1: 编辑任务契约

**Files:**
- Create: `shared/编辑任务模型.ts`
- Test: `server/tests/editor.test.ts`

- [ ] **Step 1: 写失败测试，要求空文本不能通过请求校验**

```ts
import assert from 'node:assert/strict';
import test from 'node:test';
import { assertEditorialTaskRequest, SchemaValidationError } from '../validation.js';

test('编辑任务拒绝空材料', () => {
  assert.throws(
    () => assertEditorialTaskRequest({ taskType: 'interview', sourceText: '   ' }),
    SchemaValidationError,
  );
});
```

- [ ] **Step 2: 运行失败测试**

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js`

Expected: FAIL，提示 `assertEditorialTaskRequest` 尚未导出。

- [ ] **Step 3: 定义最小请求与响应类型、Schema**

```ts
export type EditorialTaskType = 'interview' | 'news_edit';
export type NewsEditAction = 'polish' | 'structure' | 'commentary';

export interface EditorialTaskRequest {
  taskType: EditorialTaskType;
  sourceText: string;
  action?: NewsEditAction;
}

export interface EditorialTaskResult {
  taskType: EditorialTaskType;
  title: string;
  output: string;
  notes: string[];
  verificationNeeded: string[];
  generatedAt: string;
  mode: 'mock' | 'ollama' | 'qwen' | 'openai';
  fallbackNotice?: string;
}
```

`taskType: 'interview'` 只允许缺省 `action`；`taskType: 'news_edit'` 要求 `action` 为 `polish`、`structure` 或 `commentary`。Schema 为 `sourceText` 设置 `minLength: 20` 与 `maxLength: 30000`。

- [ ] **Step 4: 在 `server/validation.ts` 编译 Schema 并实现断言**

```ts
export function assertEditorialTaskRequest(value: unknown): asserts value is EditorialTaskRequest {
  if (!validateEditorialTaskRequest(value)) {
    throw new SchemaValidationError('编辑任务输入', validateEditorialTaskRequest.errors);
  }
}
```

- [ ] **Step 5: 重新运行测试**

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js`

Expected: PASS。

- [ ] **Step 6: 提交契约**

```bash
git add shared/编辑任务模型.ts server/validation.ts server/tests/editor.test.ts
git commit -m "feat: add editorial task contract"
```

### Task 2: 提示词与本地 Mock 结果

**Files:**
- Create: `prompts/编辑任务提示词.ts`
- Modify: `shared/编辑任务模型.ts`
- Test: `server/tests/editor.test.ts`

- [ ] **Step 1: 写失败测试，要求采访任务提示词禁止虚构引语**

```ts
import { buildEditorialTaskPrompt } from '../../prompts/编辑任务提示词.js';

test('采访稿提示词明确禁止编造采访内容', () => {
  const prompt = buildEditorialTaskPrompt({ taskType: 'interview', sourceText: '记者：你怎么看？\n受访者：我觉得变化很大。' });
  assert.match(prompt.system, /不得编造受访者、引语、经历、时间或事实/u);
});
```

- [ ] **Step 2: 运行失败测试**

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js`

Expected: FAIL，提示模块不存在。

- [ ] **Step 3: 实现任务专属提示词**

采访任务提示词要求输出标题、采访稿、编辑说明、待核实项，并把原话保留为候选引语。新闻编辑提示词按 `action` 分支：`polish` 保持事实不变；`structure` 调整导语和段落；`commentary` 给出评论题目、中心论点、配发位置，禁止复述整篇新闻。

```ts
const sharedBoundary = '只使用用户提供的材料；不得编造受访者、引语、经历、时间或事实。';
export const buildEditorialTaskPrompt = (input: EditorialTaskRequest) => ({
  system: input.taskType === 'interview' ? `你是新闻采访稿编辑。${sharedBoundary}` : `你是新闻编辑。${sharedBoundary}`,
  user: JSON.stringify(input),
});
```

- [ ] **Step 4: 实现确定性 Mock**

在 `shared/编辑任务模型.ts` 中导出 `createMockEditorialTaskResult`，仅截取用户原文片段形成输出，`notes` 固定标注“以下内容基于用户提供材料整理”，`verificationNeeded` 固定要求核对身份、时间和可引用原话。

- [ ] **Step 5: 重新运行测试**

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js`

Expected: PASS。

- [ ] **Step 6: 提交提示词与 Mock**

```bash
git add prompts/编辑任务提示词.ts shared/编辑任务模型.ts server/tests/editor.test.ts
git commit -m "feat: add task-specific editorial prompts"
```

### Task 3: 编辑服务与 API

**Files:**
- Create: `server/services/editor.ts`
- Create: `server/routes/editor.ts`
- Modify: `server/app.ts`
- Test: `server/tests/editor.test.ts`
- Test: `server/tests/editorApi.test.ts`

- [ ] **Step 1: 写失败测试，要求新闻编辑请求返回对应动作**

```ts
test('新闻编辑保留用户选择的编辑动作', async () => {
  const result = await service.run({ taskType: 'news_edit', action: 'commentary', sourceText: validArticle });
  assert.equal(result.taskType, 'news_edit');
  assert.match(result.output, /评论/u);
});
```

- [ ] **Step 2: 运行失败测试**

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js`

Expected: FAIL，提示 `EditorialService` 不存在。

- [ ] **Step 3: 实现 `EditorialService`**

服务接收已有 `GenerationProvider`。对 `mock` Provider 返回 `createMockEditorialTaskResult`；对 Qwen/Ollama/OpenAI 使用 `buildEditorialTaskPrompt` 调用 Provider 的结构化请求适配器，解析为 `EditorialTaskResult`，失败时返回 Mock 与 `fallbackNotice`。输出文本必须包含用户输入中的至少一个非空片段；否则以安全错误降级，防止空泛或无材料结果。

- [ ] **Step 4: 实现 `POST /api/editor`**

复用 `createGenerateRouter` 的 JSON Content-Type、每分钟限流和并发限制模式。路由调用 `assertEditorialTaskRequest`，成功返回 `EditorialTaskResult`，非法输入返回 400。

- [ ] **Step 5: 在 `server/app.ts` 挂载路由**

```ts
app.use('/api/editor', createEditorRouter(editorService, options));
```

构建 `editorService` 时使用与现有生成服务相同的主 Provider 与 Mock fallback Provider。

- [ ] **Step 6: 写 API 失败测试并确认**

```ts
test('POST /api/editor 拒绝缺少新闻编辑动作的请求', async () => {
  const response = await fetch(baseUrl + '/api/editor', {
    method: 'POST', headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ taskType: 'news_edit', sourceText: validArticle }),
  });
  assert.equal(response.status, 400);
});
```

Run: `npm run build:server && node --test dist-server/server/tests/editor.test.js dist-server/server/tests/editorApi.test.js`

Expected: PASS。

- [ ] **Step 7: 提交服务与 API**

```bash
git add server/services/editor.ts server/routes/editor.ts server/app.ts server/tests/editor.test.ts server/tests/editorApi.test.ts
git commit -m "feat: add editorial task api"
```

### Task 4: 浏览器服务与 MVP 组件

**Files:**
- Create: `src/services/编辑服务.ts`
- Create: `src/components/双功能MVP.tsx`
- Create: `src/components/双功能MVP.test.tsx`
- Modify: `src/App.tsx`
- Modify: `src/styles.css`

- [ ] **Step 1: 写失败界面测试，要求初始页显示两个任务入口**

```tsx
it('显示生成采访稿与新闻编辑入口', () => {
  render(<DualWorkflowMvp mode="mock" />);
  expect(screen.getByRole('button', { name: '生成采访稿' })).toBeTruthy();
  expect(screen.getByRole('button', { name: '新闻编辑' })).toBeTruthy();
});
```

- [ ] **Step 2: 运行失败测试**

Run: `npm run test:ui -- src/components/双功能MVP.test.tsx`

Expected: FAIL，提示组件不存在。

- [ ] **Step 3: 实现浏览器编辑服务**

`runEditorialTask` 向 `/api/editor` 发送 JSON，超时或非成功响应时调用 `createMockEditorialTaskResult`。不在浏览器保存材料，不添加本地项目或历史记录。

- [ ] **Step 4: 实现 `DualWorkflowMvp`**

初始状态显示两个按钮。选择“生成采访稿”后，显示一个材料输入框和“整理采访稿”按钮；选择“新闻编辑”后，显示同一输入框、三个动作按钮与“开始编辑”按钮。材料少于 20 字时显示具体错误。结果页显示标题、输出正文、编辑说明和待核实项，并显示“重新开始”。

- [ ] **Step 5: 写并运行交互测试**

```tsx
it('新闻编辑选择评论配发后将动作发送给服务', async () => {
  const user = userEvent.setup();
  render(<DualWorkflowMvp mode="mock" />);
  await user.click(screen.getByRole('button', { name: '新闻编辑' }));
  await user.click(screen.getByRole('button', { name: '评论配发' }));
  await user.type(screen.getByLabelText('新闻稿或评论草稿'), validArticle);
  await user.click(screen.getByRole('button', { name: '开始编辑' }));
  expect(await screen.findByText('编辑说明')).toBeTruthy();
});
```

Run: `npm run test:ui -- src/components/双功能MVP.test.tsx`

Expected: PASS。

- [ ] **Step 6: 用组件替换 `src/App.tsx` 的旧选题工作台**

```tsx
export default function App() {
  return <div className="mvp-shell"><Header reportingMode="course" /><DualWorkflowMvp mode={clientGenerationMode} /></div>;
}
```

- [ ] **Step 7: 添加响应式样式**

保留现有配色。桌面用两列任务卡，窄屏单列；输入和结果区域最多 820px 宽。为选中任务、编辑动作、加载、错误和键盘焦点提供可见状态。

- [ ] **Step 8: 运行组件测试**

Run: `npm run test:ui -- src/components/双功能MVP.test.tsx`

Expected: PASS。

- [ ] **Step 9: 提交前端**

```bash
git add src/services/编辑服务.ts src/components/双功能MVP.tsx src/components/双功能MVP.test.tsx src/App.tsx src/styles.css
git commit -m "feat: replace topic planner with dual workflow mvp"
```

### Task 5: 端到端验证

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 更新 README 的首段和运行说明**

将产品说明改为“基于已有采访材料生成采访稿，或编辑已有新闻稿与评论配发”，并写明本地 Qwen 处理材料的隐私边界；删除首页仍以选题策划为入口的表述。

- [ ] **Step 2: 运行完整自动测试**

Run: `npm test`

Expected: 所有服务端和 UI 测试通过。

- [ ] **Step 3: 运行生产构建**

Run: `npm run build`

Expected: TypeScript 与 Vite 构建成功。

- [ ] **Step 4: 浏览器主路径验证**

启动 `npm run dev`，在 `http://127.0.0.1:5173/` 依次验证：采访稿材料提交、新闻编辑的语言润色、结构修改、评论配发；确认结果页只出现各自任务的输出，没有“选题”“采访对象”或未提供的事实。

- [ ] **Step 5: 提交文档与验证结果**

```bash
git add README.md
git commit -m "docs: describe dual workflow mvp"
```
