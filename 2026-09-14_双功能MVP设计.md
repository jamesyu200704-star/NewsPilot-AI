# NewsPilot 双功能 MVP 设计

## 目标

将首页收缩为两个独立入口：生成采访稿与新闻编辑。用户先选择任务，再提供已有材料；系统不从选题推导采访对象，也不把未经提供的内容写成事实。

## 入口一：生成采访稿

用户粘贴真实采访转写、采访笔记或问答记录。输入区域提示用户保留受访者身份、提问、回答和现场细节。

本地模型输出：标题建议、导语、按新闻阅读顺序整理的采访稿、可直接引用但仍待人工复核的原话、待核实或缺失的信息。

当输入过短或不含实质采访材料时，页面不调用生成，提示补充真实采访内容。模型不得编造受访者、经历、引语、时间、地点或事实。

## 入口二：新闻编辑

用户粘贴已有新闻稿、消息稿或新闻评论草稿，并选择一种编辑动作：语言润色、结构修改、评论配发。

语言润色输出保持原意的修改稿和主要语言调整；结构修改输出调整后的稿件及导语、段落顺序和缺失信息提示；评论配发输出适合配发的评论角度、标题建议、放置位置和避免重复新闻事实的提醒。

模型只编辑用户提供的文本，不得补充没有出处的赛果、数字、人物身份或引语。

## 界面

首页只保留两个任务卡。用户点击其中一个后，页面展开对应的输入区、提交按钮和一条简短边界提示。生成结果替换输入区下方的空白区域，并提供“重新开始”。

高级设置、项目管理、采访执行、证据台账、导出和盲评工具不在 MVP 主界面出现。

## 实现

前端新增任务类型状态和两个表单。服务端生成请求新增 `taskType` 和 `sourceText` 字段，使用不同的结构化提示词，但复用现有 Ollama/Qwen 请求、超时、降级和安全校验。

新增最小结构化结果契约：采访稿结果与新闻编辑结果分别校验。生成失败时保留用户输入并显示可重试提示。

## 验证

- 采访稿：真实采访文本能生成采访稿；空文本和短文本被阻止；结果不虚构引语。
- 新闻编辑：三种编辑动作都能生成对应结果；结果不添加未提供的事实。
- 本地 Qwen 成功与失败降级路径可用。
- 运行单元测试、构建，并在浏览器走通两条主路径。
