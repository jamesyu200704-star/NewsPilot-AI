# 安全策略

NewsPilot AI 重视服务端密钥、用户输入、生成结果完整性和 API 可用性。请负责任地报告安全问题，不要在公开渠道披露可利用细节。

## 受支持版本

| 版本 | 安全更新 |
| --- | --- |
| 0.1.x | 支持 |
| 低于 0.1.0 | 不支持 |

当前项目处于 V0.1 Open Source Release。安全修复以最新的 0.1.x 版本为准。

## 报告安全问题

优先使用仓库 **Security** 页面中的 **GitHub Private Vulnerability Reporting**。如果该功能尚未启用，请使用代码托管平台上由项目维护者明确提供的私有联系渠道。

不要把以下内容提交到公开 Issue、Discussion、PR、截图或日志：

- 可直接利用的漏洞细节或完整 PoC
- 真实 OpenAI API Key、令牌、Cookie 或其他凭据
- 电话、邮箱、身份证号、学号等个人信息
- 未授权的采访材料、用户输入或生产日志

如果暂时找不到私有渠道，可以在公开 Issue 中仅请求维护者启用私有报告方式，不要附带漏洞细节、秘密或个人信息。

## 报告内容

在不包含真实敏感数据的前提下，尽量提供：

- 受影响版本、提交或文件位置
- 问题类型和可能影响
- 最小复现步骤
- 攻击前提与信任边界
- 已脱敏的日志、请求或测试输入
- 可行的修复或缓解建议

使用明显不可用的占位符，例如 <code>test-key-placeholder</code>。不要为了证明漏洞而访问不属于你的系统、数据或账号。

## 密钥与个人信息

- 真实密钥只能存放在未提交的 <code>.env.local</code>、<code>.env</code> 或部署平台服务端 Secret。
- 不得创建 <code>VITE_OPENAI_API_KEY</code>，也不得给任何秘密加 <code>VITE_</code> 前缀。
- 不得把真实秘密写入源码、测试、README、Issue、PR、构建产物或客户端日志。
- 测试数据必须使用合成值，不包含真实姓名、电话、邮箱、身份证号或学号。
- 如果秘密已经暴露，应立即在服务提供方撤销并轮换，同时检查 Git 历史、构建产物、日志和缓存；仅删除当前文件不足以完成处置。

## 当前安全边界

- 浏览器本地 Mock 不发送用户输入。
- API 模式会把输入发送到 Generator Service；OpenAI 模式还会发送到 OpenAI API。
- 服务端 OpenAI 请求使用固定 HTTPS 端点、Authorization 请求头、超时、<code>store=false</code> 和严格结构校验。
- 当前 Node.js HTTP 服务提供 JSON Content-Type 检查、单进程请求窗口和并发上限，但没有账号、鉴权、分布式配额、TLS 或生产监控。
- 公网部署必须由反向代理或 API 网关提供 TLS、访问控制、按身份分布式限流、成本保护和日志脱敏。
- 生成内容是策划建议，不是已经完成的采访、调查或事实核验。
- 项目目前没有数据库、文件上传、动态命令执行或多租户隔离。

## 负责任披露

请给维护者合理的验证和修复时间，再公开讨论已确认的问题。维护者会根据可复现性、影响和受支持部署边界评估报告，但当前文档不承诺固定响应时限。
