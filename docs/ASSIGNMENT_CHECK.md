# 作业与提交检查

检查项包括：作业类型、截止时间、最低采访数、信源多样性、人物入口、不同观点、场景、采访提纲、采访总结、字数记录、关键主张、来源冲突、未确认引语、off-record、隐私、文件格式和文件名。

状态：

- `passed`：已有当前项目记录证明满足。
- `warning`：需要人工判断或导出时保护。
- `blocking`：课程提交前必须解决。
- `not_applicable`：当前任务不适用。

检查没有综合分。最低采访数、关键主张、未完成提纲、未确认引语和开放冲突等阻断项会单独显示并阻止课程提交包生成。
