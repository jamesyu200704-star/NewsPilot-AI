# 证据支撑的报道提纲

支持人物特稿、校园现象、政策执行、数据调查、硬新闻和变化报道六种骨架。

每一节必须说明：本节目的、绑定主张、已确认证据、可用引语、剩余缺口和写作提示。`supported / partial / unsupported / conflicted` 由确定性规则重新计算。

## 约束

- 无证据主张显示为 unsupported，不能靠措辞升级。
- 假设保留为假设，不写成结论。
- 未确认、off-record、私密或已撤回引语不可绑定为可用引语。
- 关键冲突保持 visible，不能自动选择一方。
- 提纲只组织已有材料，不生成完整新闻稿或虚构场景。
